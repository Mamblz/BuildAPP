<UserControl x:Class="BuildFlowApp.Views.MainControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:BuildFlowApp.Views"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800"
             Background="#f0f2f5">
    <Grid Margin="20">
        <StackPanel>
            <TextBlock Text="Статус постройки"
                       FontSize="24"
                       FontWeight="Bold"
                       Margin="0 0 0 20"/>

            <ItemsControl x:Name="BuildingsList">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="Gray"
                                BorderThickness="1"
                                Padding="10"
                                Margin="0 0 0 10"
                                CornerRadius="5"
                                Background="White">
                            <StackPanel>
                                <TextBlock Text="{Binding Name}"
                                           FontSize="18"
                                           FontWeight="Bold"/>

                                <TextBlock>
                                    <Run Text="Статус: " FontWeight="Bold"/>
                                    <Run Text="{Binding Status}"/>
                                </TextBlock>

                                <TextBlock Text="Прогресс:"/>
                                <ProgressBar Value="{Binding Progress}"
                                             Maximum="100"
                                             Height="20"
                                             Margin="0 0 0 10"/>

                                <TextBlock Text="Необходимые ресурсы:"
                                           FontWeight="Bold"
                                           Margin="0 5 0 0"/>
                                <ItemsControl ItemsSource="{Binding BuildingResources}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding ResourceInfo}" />
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </Grid>
</UserControl>